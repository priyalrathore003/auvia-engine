# Auvia Engine — dev/langgraph
# Core
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
pydantic>=2.7.0

# LangGraph + LangChain
langgraph>=0.2.0
langchain-core>=0.2.0
langchain-groq>=0.1.6         # primary LLM (free)
langchain-openai>=0.1.0       # fallback
langchain-anthropic>=0.1.0    # restore when credits back

# RAG
chromadb>=0.5.0
sentence-transformers>=3.0.0  # all-MiniLM-L6-v2

# DSP
librosa>=0.10.0
noisereduce>=3.0.0
soundfile>=0.12.0
numpy>=1.26.0

# Utilities
httpx>=0.27.0
aiofiles>=23.2.1